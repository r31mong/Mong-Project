---
- name: "show all variables"
  hosts: all
  connection: network_cli
  gather_facts: false

  tasks:
  - name: "TASK1 - show all variables"
    ios_facts:
      gather_subset: all
      #- '!config' #Gather all except config
      gather_network_resources: all
  - debug:
      msg: "{{ hostvars[inventory_hostname] }}"